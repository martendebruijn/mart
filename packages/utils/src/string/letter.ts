/**
 * Retrieves the character at the specified index from a given string.
 *
 * @param string - The string from which to retrieve the character.
 * @param index - The zero-based index of the character to retrieve.
 * @returns The character at the specified index in the string.
 */
export function letter(string: string, index: number): string {
  return string[index];
}
